apiVersion: v1
kind: Namespace
metadata:
  name: agrosense
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agrosense-identity-api
  namespace: agrosense  
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agrosense-identity-api
  template:
    metadata:
      labels:
        app: agrosense-identity-api
    spec:
      containers:
      - name: agrosense-identity-api
        image: klurgs/agrosense-identity:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        envFrom:
        - configMapRef:
            name: agrosense-app-config 
        - secretRef:
            name: agrosense-app-secrets
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: agrosense-identity-api
  namespace: fgc
spec:
  type: ClusterIP  # Internal apenas, acessível via Gateway
  selector:
    app: agrosense-identity-api
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agrosense-identity-api-hpa
  namespace: agrosense
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agrosense-identity-api
  minReplicas: 1
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50